<div class="comments mx-5">
  <h3>Comments</h3>
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date createdAt}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>